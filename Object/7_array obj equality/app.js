let num = [1,2,3];
let mySte = [1,2,3];

// two arraies are equaled only if they reference to the same memory equal

// so num == mySte  returns false

let moreNums = nums;

// so moreNums === nums    returns true 