// object :group associate data together

// collection of property
// properties are a key-value pair
// rather than accessing data using an index, we use custom keys
const fitBitData = {
    totalSteps: 308727,
    totalMiles :211.7,
    avgCalBurn:5755,
    workoutsThisWeek: '5 of 7'
};