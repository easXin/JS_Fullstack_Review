// Accept a call back function

// Calls the function once per element in the array


const nums =  [9,8,7,2];

nums.forEach(function(n){
    console.log(n*n);
});

// num.forEach(function(num,idex){});

// the function is called one time for each element